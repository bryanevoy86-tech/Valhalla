// Auto-generated by Heimdall
import { Router } from "express";
const router = Router();

router.get("/{{ name }}", async (req, res) => {
  {% if auth_required %}// TODO: add auth middleware{% endif %}
  return res.json({ ok: true, route: "{{ name }}" });
});

export default router;
